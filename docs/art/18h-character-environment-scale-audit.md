# 18H — аудит масштаба персонажей и интерактивной мебели

Измерено `tools/art/measureRigHeight.mjs` (FK bone-space, bind-pose) — надёжнее
`measureCharacterHeight.mjs`, у которого расчёт world-scale по цепочке
предков даёт заниженный результат на текущих Meshy-риггах (0.015 м) из-за
дополнительного scale-фактора вне цепочки `nodes[].scale`, который тот скрипт
не учитывает; не использовать его для будущих измерений без починки.

## Рост персонажей (bind-pose, м)

| Персонаж | Файл | Рост |
|---|---|---|
| Соня Соколова (female_pm) | public/character/female_pm/idle.glb | 1.501 |
| Алина Белова | public/character/alina_belova/idle.glb | 1.508 |
| Кирилл Морозов | public/character/kirill_morozov/idle.glb | 1.720 |
| Илья Власов (cybersecurity) | public/character/cybersecurity/idle.glb | 1.715 |
| Игрок / гость (business_man) | public/character/business_man/idle.glb | 1.778 |
| Нарушитель (intruder) | public/character/intruder/idle.glb | 1.778 |
| Охрана 1 (security_1) | public/character/security_1/idle.glb | 1.751 |
| Охрана 2 (security_2) | public/character/security_2/idle.glb | 1.796 |

Разброс 1.50–1.80 м — правдоподобный человеческий диапазон, мужская половина
не занижена и не завышена относительно женской (жалоба «женщины визуально
больше мужчин» была устранена более ранней итерацией 18-серии, до Wave 1;
эти цифры подтверждают текущее состояние, а не описывают факт устранения).

## Мебель и интерактивные точки (факт из кода, не переоценка на глаз)

| Объект | Файл | Высота сиденья/поверхности | Соответствие ориентиру §10 | Интерактивность |
|---|---|---|---|---|
| Рабочее кресло | Chair.tsx `SEAT_HEIGHT` | 0.46 м | в диапазоне 0.43–0.48 | `CLICK_SEAT`/`CLICK_WORKSTATION`, есть |
| Кресло руководителя | CaptainChair.tsx `SEAT_TOP` | 0.49 м | у верхней границы (сознательно — комментарий в коде: подогнано под sit-клип, а не под линейку) | `CLICK_SOFA`-класс seat, player-only (16 §доп.) |
| Диван | Sofa.tsx (leg 0.12 + cushion) | ≈0.30–0.36 м (эффективная посадка ниже кресла — ожидаемо для дивана) | ниже диапазона §10, но диапазон писан для рабочих кресел, не диванов — не дефект | `CLICK_SOFA`, есть |
| Барный стул | BarStool.tsx `SEAT_HEIGHT` | 0.75 м | нет ориентира (барный стул отдельно не указан числом) | **не интерактивен** — decoration-only, `useObstacle` без `InteractionTrigger`; ни один NPC туда не садится, калибровать позу не от чего |
| Рабочий стол | Desk.tsx `DESK_HEIGHT` | 0.70 м | чуть ниже 0.72–0.76 — намеренно (комментарий в коде: опущен под руки anim-клипа печати) | — (поверхность, не seat) |
| Стол переговорной | MeetingTable.tsx `HEIGHT` | 0.75 м | в диапазоне | — |
| Кухонная стойка (кофемашина) | Kitchen.tsx `CoffeeMachine position.y` | 0.90 м | в диапазоне 0.88–0.95 | `CLICK_COFFEE_MACHINE`, есть |
| Пинг-понг-стол | PingPongTable.tsx `HEIGHT` | 0.76 м | точно соответствует регламентной высоте и ориентиру §10 | **не интерактивен** до Wave 1 → добавлены `PING_PONG_TABLE_ANCHORS` (`src/interaction/interactionAnchors.ts`); клик/FSM-состояние — задача Wave 3 (activity planner) |
| Турник | PullUpBar.tsx `BAR_Y` | 2.00 м | «над головой с запасом» — для самых высоких персонажей (1.80 м) запас 0.20 м, для самых низких (1.50 м) перекладина ВЫШЕ роста с поднятыми руками (см. ниже) | **не интерактивен** до Wave 1 → добавлены `PULL_UP_BAR_ANCHORS`; FSM/анимация — Wave 3 |
| Серверная стойка | ServerRack.tsx `HEIGHT`=2.0, trigger.y | 1.0 м (панель ремонта на уровне груди/пояса) | не задан ориентиром §10, но согласован с позой ремонта, клиппинга не найдено | `CLICK_SERVER`, есть |
| Дверные проёмы | GlassPartitionWithDoor.tsx `DOOR_HEIGHT` | 2.10 м | выше самого высокого персонажа (1.796 м) на 0.30+ м | — (проход, не seat) |
| Whiteboard | scene/whiteboardSpot.ts (позиция не менялась, 16 §правило) | — (look-at объект, не seat/hand) | нет позы контакта — калибровка не требуется | look-at target, используется meeting slots |

## Находка: турник и рост коротких персонажей

Оценка по стандартным пропорциям тела (overhead reach ≈ 1.20–1.25 × рост):

| Персонаж | Рост | Расчётный overhead reach | Перекладина (2.00 м) |
|---|---|---|---|
| Кирилл / Илья (~1.72 м) | 1.72 | 2.06–2.15 м | Достижима с небольшим вытягиванием/прыжком |
| Соня / Алина (~1.50 м) | 1.50 | 1.80–1.89 м | **Не достижима стоя на полу без прыжка** |

Это не считается дефектом калибровки мебели — высота турника (2.0 м) сама по
себе реалистична и соответствует ориентиру. Это требование к будущей mount-
анимации (Wave 3, §19 «approach-ready», «reach-and-grab»): для невысоких
персонажей mount-клип должен включать полноценный прыжок/подскок к
перекладине, а не вытягивание руки от пола. Зафиксировано здесь, чтобы Wave 3
не спроектировала общий клип «на глаз» без учёта разницы в росте.

## Barrier stool — намеренно не в scope Wave 2

`BarStool` не имеет ни одного пользователя (`onSelect` нигде не передаётся,
FSM не содержит соответствующего состояния). Калибровать посадку не от чего:
без запланированной активности «сесть у стойки» это чистая декорация. Если
такая активность появится в будущей волне — калибровку `SEAT_HEIGHT=0.75`
нужно будет провести тогда, не раньше.

## Инварианты масштаба персонажа (18H §9)

Проверено кодом, а не «на глаз»:

- `src/character/CharacterModel.tsx` монтирует `<primitive object={base.scene} />`
  без единого `scale`-пропа на всём пути от группы до примитива — обёртки со
  своим масштабом не существует, так что «cinematic wrapper всегда `[1,1,1]`»
  выполняется структурно, а не по соглашению.
- Ни один файл `src/character/**/*.ts*` не пишет в `.scale` (аудит `grep`);
  единственное исключение — компенсация масштаба кости для держащейся в руке
  кружки (`CharacterModel.tsx:189`), которая масштабирует ПРОП, а не персонажа.
- Оставшийся реальный риск изменения масштаба — SCALE-треки внутри самих
  анимационных клипов (это и было первопричиной P1-бага «модель растёт во
  время диалога»). Устранено `tools/art/stripScaleTracks.mjs` для всех
  Meshy-моделей и закрыто регрессионным тестом
  `tools/art/characterIdentity.test.ts` («no animation carries SCALE tracks»).

Regression-цепочка `scale before === during === after` из §9 проверяется на
трёх уровнях: структурно (аудит выше), на уровне ассетов (`characterIdentity.test.ts`,
guard против SCALE-треков в клипах) и теперь живым regression-тестом,
превращающим сам структурный аудит в код, а не оставляющим его утверждением
в markdown: `src/character/characterScaleInvariant.test.ts` парсит исходники
`src/character/**` и падает, если где-либо появится запись в `.scale`
персонажа (кроме двух явно разрешённых held-prop компенсаций — кружка/ракетка/
телефон) или JSX `scale=` на `<CharacterModel>`/`<group>`/`<primitive>`.
Полноценный live-mount через `@react-three/test-renderer` с реальным GLTF не
взят — `useGLTF` тянет реальный `fetch` файла, которого нет в vitest/jsdom
(нет dev/preview сервера); `Office.test.tsx` по той же причине подменяет
`CharacterComponent={() => null}`, а не монтирует настоящую модель — сохраняю
согласованность с этим уже принятым решением, а не изобретаю отдельный mock
GLTF ради одного теста. Отдельный E2E-скриншот «масштаб визуально одинаков
до/после сцены» — в scope Wave 4 (§24, пункт 5), там же, где полный visual
regression набор, а не изобретается заново здесь.
