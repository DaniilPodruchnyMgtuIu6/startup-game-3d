# Implementation notes — external-ai-data-disclosure

- Insert clip: `public/cutscenes/external-ai-data-disclosure-insert.mp4` — **сгенерирован и интегрирован**. Кейфрейм de293cc0-2d4e-420a-a671-1b55ae8df907 (nano_banana_2) → видео f8d9f93d-84ee-4aff-9357-1b3496e321cd (kling3_0_turbo), детали в `assets/source/prompts/story-clips-cyber-incidents-19b.md`. Ни реального AI-бренда, ни лиц/имени персонажа в кадре — только обобщённый чат-интерфейс и замазанный лог. Fallback (`playInsert`) остаётся полностью функциональным на случай недоступности файла/кодека.
- Камера: переиспользован `beginConversationCinematic({pairA: PLAYER_ID, pairB: characterId})` — впервые в Feature 19 с Алиной как ведущей.
- Диалог нигде не называет реальный AI-сервис/бренд — везде generic "ИИ-помощник"; покрыто тестом (`cyberStoryDialogues.test.ts`, проверка на отсутствие строк chatgpt/openai/claude/anthropic/gemini/copilot/deepseek).
- `unrestricted-ai-recurrence` (последствие risky-выбора) явно НЕ утверждает, что модель обучилась на данных команды — реплики используют формулировки "не можем точно сказать" / "не знаем точно"; покрыто тестом.
- `allow-unrestricted-ai-tools` даёт ОГРАНИЧЕННОЕ одноразовое ускорение через существующий `useProductStore.applyStoryEffortReduction` (тот же use-case, что у Feature 17B `keep-shared-architecture`) — не бесконечный бафф, не новый механизм.
- `ban-external-ai-tools` создаёт мягкий beat `shadow-ai-personal-use` (команда тихо продолжает пользоваться личными инструментами без рабочей альтернативы) БЕЗ дополнительного расхода/риск-сигнала — намеренно не вторая кара поверх более безопасного выбора (`cyberStoryConsequences.ts`, покрыто тестом).
- Связь с Feature 19A: если `shadow-it-log-upload` уже решена через `configure-secure-log-sharing`, sensitive-data impact от `allow-unrestricted-ai-tools` ниже (`hasSecureLogMasking`, `cyberStoryLinkedEffects.ts`, покрыто тестом).
- Связь с Feature 17A: если пройдено `prioritize-security-training`, диалог показывает одну дополнительную реплику Сони о том, что команда уже разбирала этот вопрос на обучении (переиспользован существующий `CyberStorySceneContext.securityTrainingCompleted`, отдельного нового селектора не создавалось).
- Проверено живым браузером (`npm run dev`, dev launcher, Playwright-скрипт вне репозитория): полный диалог + выбор `configure-controlled-ai-gateway` → `status === 'resolved'`, `flags.shadowAiPolicy === 'controlled-gateway'`, 0 console errors. Insert-клип отдельно подтверждён: `<video>` монтируется с правильным `src`, `readyState: 1`, `error: null`.
