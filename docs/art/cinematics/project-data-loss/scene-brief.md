# «Утро без проекта» — cinematic brief (18F Wave 1)

Катастрофа потери рабочих файлов (17C, consequence `project-files-destroyed`).
После сцены регистрируется поражение `unrecoverable-project-data-loss` и
свет офиса уходит в состояние `failure` (18E), все экраны гаснут (§5).
