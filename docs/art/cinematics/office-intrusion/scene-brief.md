# Office intrusion — cinematic brief (18F Wave 1)

Посторонний в офисе (Feature 10, `officeIntrusion.ts`). Тон: саспенс без
насилия; свет — состояние `security-alert` уже активно (угроза armed, 18E).
Ветки различаются маршрутом, шотами и текстом: с Ильёй — перехват у входа;
без него — чужой у рабочего экрана.
