# Lighting — office intrusion

Состояние `security-alert` (18E) активно с момента armed-угрозы: приглушённый
холодный ключ + янтарный акцент. Отдельного света сцена не ставит; после
resolveIntrusion угроза снимается и свет возвращается сам (§6 «cinematic
state не остаётся»).
