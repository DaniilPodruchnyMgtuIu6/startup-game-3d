# Implementation notes — secret-committed-to-repository

- Insert clip: `public/cutscenes/secret-committed-to-repository-insert.mp4` — **сгенерирован и интегрирован**. Кейфрейм b8ee584d-2f5a-43fe-b88d-cfe9fa71bead (nano_banana_2) → видео 4c9eba2f-9160-4281-9c2f-fce9372cac0a (kling3_0_turbo), детали в `assets/source/prompts/story-clips-cyber-incidents-19b.md`. Fallback (`playInsert`) остаётся полностью функциональным на случай недоступности файла/кодека.
- Камера: переиспользован `beginConversationCinematic({pairA: PLAYER_ID, pairB: characterId})` — тот же примитив, что у всех Feature 19 сцен, никаких новых camera-примитивов не создано.
- Три выбора математически различаются по домену `credentialExposureState` (`history-retained` / `reduced` / `rotated`), а не просто по флагу true/false — реализовано в `cyberStoryHandlers.ts::resolveSecretChoice`, покрыто `cyberStoryHandlers.test.ts`.
- Проверено живым браузером (`npm run dev`, dev launcher, Playwright-скрипт вне репозитория): полный диалог + выбор `rotate-and-secure-secret` → `incidents['secret-committed-to-repository'].status === 'resolved'`, `flags.credentialExposureState === 'rotated'`, 0 console errors. Insert-клип отдельно подтверждён: `<video>` монтируется с правильным `src`, `readyState: 4`, `error: null`.
- Найденный и исправленный по ходу баг (до генерации видео, чисто логический): `rotate-and-secure-secret` без Ильи вызывал `occupy(ctx, 'kirill-morozov', ...)` дважды с одинаковым `operationId:employeeId` — второй вызов тихо отбрасывался `addAssignmentOnce`'s dedup-проверкой, из-за чего Кирилл получал только базовые дни без "без-Ильи" надбавки. Исправлено объединением в один вызов `occupy` с суммой дней (тот же паттерн, что уже был у `enable-phishing-resistant-auth`). Тот же баг был и в `resolveExternalAiChoice`'s `configure-controlled-ai-gateway` — исправлен аналогично.
